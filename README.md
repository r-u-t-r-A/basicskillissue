Simple python script to track oscillation times on LPF labs since counting them manually is annoying
To use the script you have to record a video of the pendolum/whatever oscillating and run the script with the video

Requiremnts for the script to run: opencv-contrib-python scipy matplotlib numpy
